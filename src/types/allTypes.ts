export type MatrixType = {
    image: string;
    price: string;
    description: string;
}
export type OwnerType = {
    image: string;
    name: string;
    description: string;
}
export type ServiceType = {
    image: string;
    name: string;
    description: string;
}